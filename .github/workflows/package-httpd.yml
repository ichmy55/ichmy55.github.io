name: Build the package for textlint and testing.

on:
  workflow_call:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    name: Build the docker image for textlint and testing
    runs-on: ubuntu-26.04
    permissions:
      contents: read
      packages: write
    steps:
      - name: "Checkout  repository"
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v47

      - name: "Set up Docker Buildx"
        if: contains(steps.changed-files.outputs.all_modified_files, 'Dockerfile')
        uses: docker/setup-buildx-action@v4
 
      - name: "Container registry に認証する"
        if: contains(steps.changed-files.outputs.all_modified_files, 'Dockerfile')
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: "Extract metadata (tags, labels) for Docker"
        if: contains(steps.changed-files.outputs.all_modified_files, 'Dockerfile')
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/httpd
 
      - name: "Docker ビルド & プッシュ"
        if: contains(steps.changed-files.outputs.all_modified_files, 'Dockerfile')
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
