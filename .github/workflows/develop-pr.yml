name: testlint at PR to development

on:
  pull_request:
    branches:
      - development

permissions:
  contents: read
  pull-requests: write

jobs:
  textlint-reviewdog:
    uses: ./.github/workflows/textlint-reviewdog.yml
