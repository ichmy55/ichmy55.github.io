name: develop push

on:
  push:
    branches:
      - development

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-26.04
    steps:
      - run: echo "This is a placeholder workflow."
